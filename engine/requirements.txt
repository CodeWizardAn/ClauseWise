# ClauseWise engine sidecar — text extraction and PII redaction.
# Pinned so a rebuild cannot silently change behaviour.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pymupdf==1.28.2
python-docx==1.2.0

# PII redaction (Phase 1). The spaCy model is installed from its release URL
# because it is not published on PyPI.
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
en_core_web_md @ https://github.com/explosion/spacy-models/releases/download/en_core_web_md-3.8.0/en_core_web_md-3.8.0-py3-none-any.whl

# Multilingual (Phase 5) is NOT here on purpose. torch and transformers are
# needed only to rebuild the translation catalogue, never to serve it — the app
# reads the prebuilt catalog.json. Keeping them out takes the deployed image
# from ~3 GB to ~400 MB, which is what lets it run on a free tier at all.
# To rebuild the catalogue locally: pip install -r requirements-translate.txt
